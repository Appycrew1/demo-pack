<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Booklet</title>
  <style>
    @page { size: A4; margin: 0; }
    html, body { margin: 0; padding: 0; }
    @font-face {
      font-family: 'Inter';
      src: url('/assets/fonts/Inter-Variable.ttf') format('truetype');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }
    :root{
      --brand-primary: {{BRAND.primary}};
      --brand-on-primary: {{BRAND.textOnPrimary}};
    }
    body { font-family: Inter, system-ui, Arial; color: #111; }
    .page { position: relative; width: 210mm; height: 297mm; box-sizing: border-box; padding: 20mm; }
    .cover { background: var(--brand-primary); color: var(--brand-on-primary); display: flex; flex-direction: column; justify-content: center; align-items: center; }
    .logo { width: 80mm; height: auto; margin-bottom: 10mm; }
    h1 { font-size: 28pt; margin: 0; }
    h2 { margin-top: 14pt; font-size: 16pt; opacity: 0.9; }
    .content { padding: 20mm; }
    .accent { height: 6mm; background: var(--brand-primary); margin: 0 20mm 10mm; }
  </style>
</head>
<body>
  <section class="page cover">
    <img class="logo" src="{{LOGO_DATAURL}}" alt="Logo" />
    <h1>{{COMPANY_NAME}} — Product Walkthrough</h1>
    <h2>Your custom sample package</h2>
  </section>

  <section class="page">
    <div class="accent"></div>
    <div class="content">
      <h2 style="color: var(--brand-primary); margin-top:0">Welcome</h2>
      <p>This booklet gives {{COMPANY_NAME}} a simple walkthrough of the application and shows how it fits a moving company workflow.</p>
      <h3 style="color: var(--brand-primary)">Key Features</h3>
      <ul>
        <li>Job scheduling & crew assignment</li>
        <li>Digital forms & signatures</li>
        <li>Real-time updates for dispatch</li>
        <li>Invoices & payments</li>
      </ul>
    </div>
  </section>
</body>
</html>
