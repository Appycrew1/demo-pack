<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Forms</title>
  <style>
    :root{
      --brand-primary: {{BRAND.primary}};
      --brand-on-primary: {{BRAND.textOnPrimary}};
    }
    @page { size: A4; margin: 10mm; }
    body { font-family: Inter, system-ui, Arial; color: #111; }
    header { display:flex; align-items:center; gap:12mm; margin-bottom:10mm; }
    .logo { height: 18mm; width:auto; }
    .title { font-size: 18pt; color: var(--brand-primary); margin:0; }
    .field { border: 1px solid #bbb; padding: 6mm; margin-bottom: 6mm; border-left: 3mm solid var(--brand-primary); }
    .label { font-size: 10pt; color: #666; margin-bottom: 2mm; }
  </style>
</head>
<body>
  <header>
    <img class="logo" src="{{LOGO_DATAURL}}" alt="Logo" />
    <h1 class="title">{{COMPANY_NAME}} — Sample Form</h1>
  </header>

  <div class="field">
    <div class="label">Customer Name</div>
    <div>_______________________________</div>
  </div>

  <div class="field">
    <div class="label">Job Address</div>
    <div>_______________________________</div>
  </div>

  <div class="field">
    <div class="label">Signature</div>
    <div>_______________________________</div>
  </div>
</body>
</html>
